/**
 * @startingPoint section="Core" subtitle="Bordered floating box — hosts the filter list and the legend" viewport="700x160"
 */
export interface PanelProps {
  /** small uppercase mono eyebrow, e.g. "Show" */
  label?: string;
  children: React.ReactNode;
}
