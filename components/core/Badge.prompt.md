Tiny uppercase mono badge for a stop's verification status. Solid-fill only — never outline-only.

```jsx
<Badge status="verified" />
<Badge status="unverified" />
```

Used in popup meta lines and the map legend. Pair with a `Seasonal` badge (ink-soft fill) when a stop is dashed-ring/seasonal.
