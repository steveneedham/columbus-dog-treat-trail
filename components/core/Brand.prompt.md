The top-bar lockup: paw-and-compass mark + "COLUMBUS / DOG TREAT TRAIL" wordmark, with an optional mono status line beneath.

```jsx
<Brand subtitle="Stashes spotted on foot — v0.5" logoSrc="../../assets/logo-lockup.svg" />
```

Point `logoSrc` at wherever `assets/logo-lockup.svg` was copied to, relative to the consuming page.
